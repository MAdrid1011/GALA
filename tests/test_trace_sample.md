# Trace Sample Tests

Tests verify dependency closure, dense event and payload rebasing, query packet
support masks, state history, physical packet validation, configured caps, and
CLI rejection when bounded samples are used without the required scope flag.
