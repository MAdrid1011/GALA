# Input Freeze Tests

Tests cover strict configuration and source-manifest loading, dataset inventory
and geometry, training defaults and schedules, interpreter binding, random
state, quality identity, and freeze-record validation. Integration checks use
the standard ignored workspace when a pinned upstream checkout is present.
