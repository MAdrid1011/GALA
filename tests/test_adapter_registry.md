# Model Adapter Registry Tests

The registry tests require all four model identifiers and preserve the
distinction between author-source adapters and the independent GR-Gaussian
implementation. Command construction is checked without relying on the process
working directory.
