# Release Hygiene Tests

These tests scan tracked and unignored files for machine-specific paths,
non-English text, retired internal record-document names, workspace content,
and common generated artifact formats. Source packages named `trace` or
`results` remain valid because they contain implementation code rather than
generated runs.
