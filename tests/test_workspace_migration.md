# Workspace Migration Tests

The tests verify blocker handling, dry-run behavior, the atomic root rename,
known asset relocation, managed-directory creation, and preservation of legacy
artifacts under the ignored archive directory. Synthetic directories keep the
tests independent of local runtime data.
